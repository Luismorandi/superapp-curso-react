import React from "react";
import ShoppingCartIcon from '@mui/icons-material/ShoppingCart';
import "../navBarFolder/cartWidget.css"

const CartWidget = () =>{
    return (
        <ShoppingCartIcon fontSize="large" className="cart"/>
    )
}

export default CartWidget