import "../componentsContainer/itemListContainer.css"

function ItemListContainer({name, numero}){
    return(
        <h1>Hola, esto es {name} y estamos en la comisión {numero}</h1>

    )
}

export default ItemListContainer